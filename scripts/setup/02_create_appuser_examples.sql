-- Create EXAMPLES table in APPUSER schema
CREATE TABLE
    APPUSER.EXAMPLES (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        NAME VARCHAR2(100) NOT NULL,
        CREATED_BY VARCHAR2(50) NOT NULL,
        CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    )
;

-- Insert dummy records into EXAMPLES table
INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Alice', 'admin')
;

INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Bob', 'admin')
;

INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Charlie', 'system')
;

-- Commit the changes
COMMIT
;
