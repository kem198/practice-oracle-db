CREATE USER APPUSER IDENTIFIED BY __ENTER_APPUSER_PASSWORD_HERE__;

GRANT CONNECT,
RESOURCE TO APPUSER;

ALTER USER APPUSER DEFAULT TABLESPACE USERS;

CREATE TABLE
    APPUSER.EXAMPLES (
        ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        NAME VARCHAR2 (100) NOT NULL,
        CREATED_BY VARCHAR2 (50) NOT NULL
    );

INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Alice', 'admin');

INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Bob', 'admin');

INSERT INTO
    APPUSER.EXAMPLES (NAME, CREATED_BY)
VALUES
    ('Charlie', 'system');

COMMIT;
